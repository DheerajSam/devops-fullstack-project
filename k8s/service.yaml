apiVersion: v1
kind: Service
metadata:
  name: devops-fullstack-service
spec:
  type: NodePort
  selector:
    app: devops-fullstack
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30080